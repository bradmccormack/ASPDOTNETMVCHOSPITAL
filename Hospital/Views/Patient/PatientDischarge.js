creditcardjs.onValidityChange(function (isValid) {
    if (isValid) {
        /* allow form submission */
        alert("Its valid");
    } else {
       
        /* prevent form submission */
    }
});